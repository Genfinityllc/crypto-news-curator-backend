After creating your GitHub repository, run these commands:

# Add your GitHub repository as remote (replace YOUR_USERNAME with your actual GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/crypto-news-curator-backend.git

# Push to GitHub
git branch -M main
git push -u origin main

# Then go to https://railway.app to deploy